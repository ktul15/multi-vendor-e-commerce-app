import { AdminRoutePlaceholder } from "../../admin-route-placeholder";

export default function VendorsPage() {
  return (
    <AdminRoutePlaceholder
      title="Vendors"
      description="Vendor review and lifecycle management arrive in issue #106."
    />
  );
}
